import { CastumerTransaction } from './castumer-transaction';

describe('CastumerTransaction', () => {
  it('should create an instance', () => {
    expect(new CastumerTransaction()).toBeTruthy();
  });
});
